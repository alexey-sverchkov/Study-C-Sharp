<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Library</OutputType>
    <TargetFramework>netcoreapp3.1</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Labs.FileStorage.Console.Business\Labs.FileStorage.Console.Business.csproj" />
  </ItemGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="if not exist $(ProjectDir)..\Labs.FileStorage.Console.Business\lib mkdir $(ProjectDir)..\Labs.FileStorage.Console.Business\lib&#xD;&#xA;xcopy /y /d &quot;$(OutDir)Labs.FileStorage.Console.XmlExporterPlugin.dll&quot; &quot;$(ProjectDir)..\Labs.FileStorage.Console.Business\lib&quot;" />
  </Target>

</Project>
